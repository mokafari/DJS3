# Test Display Images

This directory contains reference images and patterns for display verification.

## Test Patterns

- Color bar pattern (RGB test)
- Gradient pattern
- Grid pattern for touch calibration

## Usage

These patterns are used for visual verification of display rendering during integration tests.

Note: Actual pattern generation is done programmatically in the test apps using `display_test_color_bars()` and similar functions.

